# 任务一推理依赖（管线基线 requirements.txt 之外的额外依赖）。
# CUDA 12.1 环境请用官方索引安装 torch，避免 pip 换成 CPU 版：
#   python -m pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#   python -m pip install "timm>=1.0,<2" "numpy>=1.26,<3" "nibabel>=5.2,<6"
torch>=2.2
timm>=1.0,<2
numpy>=1.26,<3
nibabel>=5.2,<6
